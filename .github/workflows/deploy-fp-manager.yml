name: Deploy fp-manager to GitHub Pages

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to deploy (e.g., 1.0.0)'
        required: true

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Set app version
        run: |
          VERSION="${{ github.event.inputs.version }}"
          echo "Deploying version: $VERSION"
          sed -i "s/APP_VERSION = '0.0.0'/APP_VERSION = '${VERSION}'/" projects/fp-manager/src/app/version.ts

      - name: Build fp-manager for production
        run: npm run ng build fp-manager -- --base-href /fp-lab/

      - name: Create 404.html for GitHub Pages routing
        run: cp dist/fp-manager/browser/index.html dist/fp-manager/browser/404.html

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist/fp-manager/browser'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
